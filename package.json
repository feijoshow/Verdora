{
  "name": "verdora",
  "version": "1.0.0",
  "private": true,
  "description": "Verdora — agricultural intelligence platform",
  "scripts": {
    "start": "npm run start --prefix frontend",
    "start:clear": "npm run start:clear --prefix frontend",
    "start:online": "npm run start:online --prefix frontend",
    "android": "npm run android --prefix frontend",
    "ios": "npm run ios --prefix frontend",
    "web": "npm run web --prefix frontend",
    "typecheck": "npm run typecheck --prefix frontend",
    "install:frontend": "npm install --prefix frontend"
  },
  "dependencies": {
    "expo": "^56.0.12",
    "react-dom": "19.2.3"
  },
  "devDependencies": {
    "@types/react": "~19.2.14",
    "typescript": "~6.0.3"
  }
}
